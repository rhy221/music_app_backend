name: CI 
on: 
"  push:" 
"    branches: [ main ]" 
jobs: 
"  main:" 
"    runs-on: ubuntu-latest" 
"    steps:" 
"      - uses: actions/checkout@v4" 
"      - run: npm ci" 
"      - run: npx nx affected --target=build" 
