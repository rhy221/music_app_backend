# ts-common

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ts-common` to build the library.

## Running unit tests

Run `nx test ts-common` to execute the unit tests via [Vitest](https://vitest.dev/).
